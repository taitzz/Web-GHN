version: '3.9'

services:
  admin:
    build: ./admin
    ports:
      - "3001:80"
    depends_on:
      - backend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:5000"
